# SJTU-CS1602-01-minimatrix
这是一个基于Python实现的矩阵计算库，提供了丰富的矩阵操作功能，包括基本的矩阵运算、线性代数计算、矩阵变换等。本项目是上海交通大学IEEE试点班计算导论课程CS1602-01的大作业，旨在通过手动实现矩阵类来加深对线性代数和Python编程的理解。

## 📖 项目简介

## ✨ 主要功能

### 基础操作
- **矩阵创建**：支持多种方式创建矩阵（列表、维度、初始值）
- **矩阵形状**：获取矩阵维度(`shape()`)、重塑矩阵(`reshape()`)
- **矩阵复制**：深拷贝矩阵(`copy()`)
- **矩阵索引**：支持元素访问和切片操作(`__getitem__`, `__setitem__`)
- **字符串表示**：美观打印矩阵(`__str__`)

### 矩阵运算
- **矩阵乘法**：`dot()`
- **矩阵加法**：`__add__`
- **矩阵减法**：`__sub__`
- **对应元素乘法**：`__mul__`
- **矩阵转置**：`T()`
- **矩阵幂运算**：`__pow__`
- **Kronecker积**：`Kronecker_product()`
- **矩阵拼接**：`concatenate()`

### 高级功能
- **高斯消元**：`gauss()`
- **行列式计算**：`det()`
- **逆矩阵求解**：`inverse()`
- **矩阵秩计算**：`rank()`
- **向量化操作**：`vectorize()`

### 实用工具
- **单位矩阵**：`I()`
- **全零矩阵**：`zeros()`, `zeros_like()`
- **全一矩阵**：`ones()`, `ones_like()`
- **随机矩阵**：`nrandom()`, `nrandom_like()`
- **序列矩阵**：`arange()`
- **多维数组**：`narray()`

## 🚀 快速开始

### 安装
无需安装，直接将`minimatrix.py`文件放入你的项目目录即可。

### 基本使用示例

```python
import minimatrix as mm

# 创建矩阵
mat = mm.Matrix([[1, 2, 3], [4, 5, 6], [7, 8, 9]])
print(mat)
# 输出：
# [[1 2 3]
#  [4 5 6]
#  [7 8 9]]

# 矩阵运算
A = mm.Matrix([[1, 2], [3, 4]])
B = mm.Matrix([[5, 6], [7, 8]])
print(A.dot(B))  # 矩阵乘法

# 转置
mat.T()
print(mat)

# 行列式
print(mat.det())

# 逆矩阵
m3 = mm.Matrix([[1, 2, 3], [3, 5, 2], [6, 8, 3]])
print(m3.inverse())

# 创建特殊矩阵
print(mm.Matrix.I(3))  # 3x3单位矩阵
print(mm.Matrix.zeros((2, 3)))  # 2x3全零矩阵
print(mm.Matrix.ones((2, 2)))  # 2x2全一矩阵
print(mm.Matrix.nrandom((3, 3)))  # 3x3随机矩阵
